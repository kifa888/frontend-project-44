import readlineSync from 'readline-sync';
import { userName, rounds } from '../index.js';

console.log('What number is missing in the progression?');

const determiningNumber = () => {
    for (let i = 0; i < rounds; i+=1) {
        
        
        



    }

}
